# pnpm build-script approval for EAS (not npm syntax)
# @clerk/shared and esbuild need scripts enabled during install
pnpm.packageExtensions.@clerk/shared.scripts=true
pnpm.packageExtensions.esbuild.scripts=true
auto-install-peers=true